# Dependencies
/node_modules

# Production
/build

# Generated files
.docusaurus
.cache-loader

# Misc
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Cloudflare Worker local secrets (Alpaca keys etc.) — never commit these
.dev.vars
**/.dev.vars
.wrangler/

npm-debug.log*
yarn-debug.log*
yarn-error.log*

docs/Programming/python.md.bak

# Private working notes & tooling — never commit (backed up in OneDrive)
CLAUDE.md
AGENTS.md
HANDOFF.md
HANDOFF-*.md
DESIGN_HANDOFF.md
MS444_STATS_GAP_REPORT.md
.claude/
.understand-anything/

# Cross-assistant handoff state (see .handoff/README.md) and per-agent dirs.
# Private working state, shared between AI sessions on this machine — not part
# of the site.
.handoff/
.openai/

# Local scratch / one-off dumps — never part of the site
git_history_calculus.txt
old_calc.txt
scratch_*.txt
scratch_*.js
