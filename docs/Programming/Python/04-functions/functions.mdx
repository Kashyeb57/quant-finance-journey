---
title: "Functions"
sidebar_position: 1
---

#### Functions in Python
Video Outline:
1. Introduction to Functions
2. Defining Functions
3. Calling Functions
4. Function Parameters
5. Default Parameters
6. Variable-Length Arguments
7. Return Statement

##### Introduction to Functions
Definition:

A function is a block of code that performs a specific task.
Functions help in organizing code, reusing code, and improving readability.

```python
## syntax
def function_name(parameters):
    """Docstring"""
    # Function body
    return expression
```

```python
## why functions?
num=24
if num%2==0:
    print("the number is even")
else:
    print("the number is odd")
```

*Output:*

```text
the number is even
```

```python
def even_or_odd(num):
    """This function finds even or odd"""
    if num%2==0:
        print("the number is even")
    else:
        print("the number is odd")
```

```python
## Call this function
even_or_odd(24)
```

*Output:*

```text
the number is even
```

```python
## function with multiple parameters

def add(a,b):
    return a+b

result=add(2,4)
print(result)
    
```

*Output:*

```text
6
```

```python
## Default Parameters

def greet(name):
    print(f"Hello {name} Welcome To the paradise")

greet("Joyeb")
```

*Output:*

```text
Hello Joyeb Welcome To the paradise
```

```python
def greet(name="Guest"):
    print(f"Hello {name} Welcome To the paradise")

greet("Joyeb")
```

*Output:*

```text
Hello Joyeb Welcome To the paradise
```

```python
### Variable Length Arguments
## Positional And Keywords arguments

def print_numbers(*Joyeb):
    for number in Joyeb:
        print(number)
```

```python
print_numbers(1,2,3,4,5,6,7,8,"Joyeb")
```

*Output:*

```text
1
2
3
4
5
6
7
8
Joyeb
```

```python
## Positional arguments
def print_numbers(*args):
    for number in args:
        print(number)
```

```python
print_numbers(1,2,3,4,5,6,7,8,"Joyeb")
```

*Output:*

```text
1
2
3
4
5
6
7
8
Joyeb
```

```python
### Keywords Arguments

def print_details(**kwargs):
    for key,value in kwargs.items():
        print(f"{key}:{value}")
```

```python
print_details(name="Joyeb",age="32",country="India")
```

*Output:*

```text
name:Joyeb
age:32
country:India
```

```python
def print_details(*args,**kwargs):
    for val in args:
        print(f" Positional arument :{val}")
    
    for key,value in kwargs.items():
        print(f"{key}:{value}")
```

```python
print_details(1,2,3,4,"Joyeb",name="Joyeb",age="32",country="India")
```

*Output:*

```text
 Positional arument :1
 Positional arument :2
 Positional arument :3
 Positional arument :4
 Positional arument :Joyeb
name:Joyeb
age:32
country:India
```

```python
### Return statements
def multiply(a,b):
    return a*b

multiply(2,3)
```

*Output:*

```text
6
```

```python
### Return multiple parameters
def multiply(a,b):
    return a*b,a

multiply(2,3)
```

*Output:*

```text
(6, 2)
```

---

## 🧪 Try it yourself

<PySandbox title="Exercise: write a compound-interest function" code={`def future_value(principal, rate, years, m=1):
    """Future value with compounding m times per year."""
    return principal * (1 + rate / m) ** (m * years)

# $1,000 at 8% for 9 years — Rule of 72 says this should roughly double
print(round(future_value(1000, 0.08, 9), 2))

# Keyword arguments make calls self-documenting
print(round(future_value(principal=5000, rate=0.05, years=10, m=12), 2))

# *args in action: average of any number of returns
def average(*returns):
    return sum(returns) / len(returns)

print(average(0.05, 0.08, -0.02, 0.11))

# Challenge: add a docstring test — call help(future_value)`} />

## ✅ Check your understanding

<Quiz questions={[
  {
    q: 'What does a function without a return statement give back?',
    options: ['0', 'The last computed value', 'None', 'An error'],
    answer: 2,
    explain: 'Every Python function returns something; with no explicit return (or a bare return), that something is None.',
  },
  {
    q: 'def f(x, y=10): … — what is f(5)?',
    options: ['An error — y is missing', 'Uses y = 10', 'Uses y = None', 'Returns 5'],
    answer: 1,
    explain: 'y=10 is a DEFAULT argument: callers may omit it. Required parameters must come before defaulted ones in the signature.',
  },
  {
    q: 'In def g(*args), what type is args inside the function?',
    options: ['list', 'tuple', 'dict', 'set'],
    answer: 1,
    explain: '*args collects extra positional arguments into a TUPLE. (**kwargs collects keyword arguments into a dict.)',
  },
  {
    q: 'Why is def f(x, items=[]) a famous Python pitfall?',
    options: [
      'Lists cannot be default values',
      'The same list object is shared across ALL calls',
      'It raises a SyntaxError',
      'items becomes a tuple',
    ],
    answer: 1,
    explain: 'Default values are evaluated ONCE at definition time. Every call that appends to items mutates the same shared list. The idiom: default to None, then items = items or [] inside.',
  },
]} />

:::tip Open scratchpad
Want to experiment freely beyond these exercises? [Open the Python Notebook →](/notebook) — numpy, pandas, matplotlib, a full Jupyter-style environment that runs entirely in your browser and saves between sessions.
:::
