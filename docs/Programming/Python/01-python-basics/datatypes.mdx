---
title: "Data Types"
sidebar_position: 3
---

## DataTypes
#### 1. Definition:

- Data types are a classification of data which tell the compiler or interpreter how the programmer intends to use the data.
- They determine the type of operations that can be performed on the data, the values that the data can take, and the amount of memory needed to store the data.

#### 2. Importance of Data Types in Programming
Explanation:

- Data types ensure that data is stored in an efficient way.
- They help in performing correct operations on data.
- Proper use of data types can prevent errors and bugs in the program.

Video Outline:
1. Introduction to Data Types
2. Importance of Data Types in Programming
3. Basic Data Types
   - Integers
   - Floating-point numbers
   - Strings
   - Booleans
4. Advanced Data Types
   - Lists
   - Tuples
   - Sets
   - Dictionaries
5. Type Conversion
6. Practical Examples

```python
## Integer Example
age=35
type(age)
```

*Output:*

```text
int
```

```python
##floating point datatype
height=5.11
print(height)
print(type(height))
```

*Output:*

```text
5.11
<class 'float'>
```

```python
## string datatype example
name="Joyeb"
print(name)
print(type(name))
```

*Output:*

```text
Joyeb
<class 'str'>
```

```python
## boolean datatype
is_true=True
type(is_true)
```

*Output:*

```text
bool
```

```python
a=10
b=10

type(a==b)
```

*Output:*

```text
bool
```

```python
## common errors

result="Hello" + 5
 
```

*Output:*

```text
TypeError: can only concatenate str (not "int") to str
```

```python
result="Hello" + str(5)
print(result)
```

*Output:*

```text
Hello5
```

---

## 🧪 Try it yourself

<PySandbox title="Exercise: type detective" code={`# Predict the type of each expression BEFORE running!
print(type(10))
print(type(10.0))
print(type(10 / 2))     # careful with this one...
print(type(10 // 3))
print(type("10"))
print(type(True))
print(type(int("42") + 8))

# Challenge: why is this False?
print(0.1 + 0.2 == 0.3)
print(0.1 + 0.2)`} />

## ✅ Check your understanding

<Quiz questions={[
  {
    q: 'What is the type of the result of 10 / 2 in Python 3?',
    options: ['int', 'float', 'str', 'It depends on the values'],
    answer: 1,
    explain: 'True division (/) ALWAYS returns a float in Python 3 — even 10 / 2 gives 5.0. Use // if you want integer (floor) division.',
  },
  {
    q: 'Why does 0.1 + 0.2 == 0.3 evaluate to False?',
    options: [
      'Python has a bug in addition',
      'Floats are stored in binary and cannot represent 0.1 exactly',
      '== does not work with floats',
      'It is actually True',
    ],
    answer: 1,
    explain: 'Floats are binary fractions; 0.1 has no exact binary representation, so tiny rounding errors accumulate (0.30000000000000004). In finance code, use decimal.Decimal or compare with a tolerance.',
  },
  {
    q: 'What does int("7") + float("2.5") produce?',
    options: ['9.5', '"72.5"', 'TypeError', '9'],
    answer: 0,
    explain: 'Both strings convert cleanly to numbers: 7 + 2.5 = 9.5 (int + float promotes to float). Without the conversions, "7" + "2.5" would concatenate to "72.5".',
  },
]} />

:::tip Open scratchpad
Want to experiment freely beyond these exercises? [Open the Python Notebook →](/notebook) — numpy, pandas, matplotlib, a full Jupyter-style environment that runs entirely in your browser and saves between sessions.
:::
